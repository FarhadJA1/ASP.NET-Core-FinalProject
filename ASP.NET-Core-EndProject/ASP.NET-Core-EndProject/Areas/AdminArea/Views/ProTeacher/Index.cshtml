@model List<ProTeacher>
@{
    ViewData["Title"] = "Professional Teacher";
    int count = 1;

}
<div class="container mt-3">
    <div class="d-flex justify-content-between">
        <h2>Professional Teachers</h2>
        <a class="btn btn-success" asp-action="Create">Create Professional Teacher</a>
    </div>
    <div class="table-responsive pt-3">
        <table class="table table-dark">
            <thead>
                <tr>
                    <th>
                        #
                    </th>
                    <th>
                        Title
                    </th>
                    <th>
                        Settings
                    </th>

                </tr>
            </thead>
            <tbody>
                @foreach (var proTeacher in Model)
                {
                    <tr>
                        <td>

                            @count
                        </td>
                        <td>
                            @proTeacher.Title
                            

                        </td>
                        <td>
                            <a class="btn btn-warning" asp-action="Update" asp-route-id="@proTeacher.Id">Edit</a>

                            <form style="display:inline-block" method="post" asp-action="Delete" asp-route-id="@proTeacher.Id">
                                <button class="btn btn-danger">Delete</button>
                            </form>
                            <a class="btn btn-info" asp-action="Detail" asp-route-id="@proTeacher.Id">Detail</a>
                        </td>

                    </tr>

                    count++;
                }

            </tbody>
        </table>
    </div>
</div>






